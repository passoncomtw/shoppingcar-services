import { connect } from "react-redux";
import ProductDetailScreen from "./view";

const mapStateToProps = ({}) => {
  return {};
};

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(ProductDetailScreen);
